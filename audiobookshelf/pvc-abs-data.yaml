apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-abs-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path  # adjust if your cluster uses another name
