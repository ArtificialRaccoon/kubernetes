apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-abs-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path  # adjust if your cluster uses another name
